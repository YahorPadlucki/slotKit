import Texture = PIXI.Texture;

export class LoaderCache {

    private imageCache: Map<string, Texture>; //todo  polyfill

    public addTexture(id: string, texture: Texture) {

    }

    public getTexture(id: string) {

    }
}